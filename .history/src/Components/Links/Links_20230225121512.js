export default function Links({type}) {

    if (type === 'Apply')
    {
            return (
            <nav>
                
            </nav>
        )
    } else {
        return (
            <nav>
                
            </nav>
        )
    }
};
